#ifndef SYNTH_PREPARE_GRID_H
#define SYNTH_PREPARE_GRID_H

void computeLMGrid(struct optionsList inOptions, 
                   struct structHeader msHeader, 
                   double lArray[], double mArray[]);

#endif
